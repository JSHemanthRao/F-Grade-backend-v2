{
  "swagger": "2.0",
  "info": {
    "title": "Zoho CRM Natural Language Assistant",
    "version": "1.0.0",
    "description": "Read-only natural-language access to live Zoho CRM data. The backend resolves module, metadata, field names, dates, filters, sorting, aggregation, comparisons, relationships, activities, pagination, and continuation. The user sends only a natural-language CRM question and the backend owns all planning and token management.",
    "x-copilot-studio-tool-description": "Use this read-only tool only for live Zoho CRM questions. FIRST REQUEST: Send the user's complete original CRM question in question. Do not create a continuation_token. PAGINATION FOLLOW-UP: When the user asks next 20, next 10, next 50, next page, next batch, show more, continue, another 20, page 2, or page 3, and a previous CRM response contains continuation_token, pass the exact continuation_token from that response. Never generate a token. Never modify a token. Never calculate an offset. Never construct COQL. Never construct module names. Never construct field API names. Never emit a Leads field named Converted. Conversion questions must remain natural language, and the backend owns pagination and query planning."
  },
  "host": "f-grade-backend-v2.onrender.com",
  "basePath": "/",
  "schemes": [
    "https"
  ],
  "consumes": [
    "application/json"
  ],
  "produces": [
    "application/json"
  ],
  "securityDefinitions": {
    "api_key": {
      "type": "apiKey",
      "in": "header",
      "name": "x-api-key"
    }
  },
  "security": [
    {
      "api_key": []
    }
  ],
  "paths": {
    "/api/crm/assistant": {
      "post": {
        "operationId": "askCrmAssistant",
        "summary": "Ask Zoho CRM",
        "x-ms-summary": "Ask Zoho CRM",
        "description": "Pass the user's complete original natural-language CRM request exactly as written. Do not rewrite, summarize, translate, interpret, construct structured CRM parameters, calculate pagination, or construct COQL. For the first request, send the user's original question only. For pagination follow-ups, pass the exact continuation_token returned by the previous CRM response. Never invent, modify, decode, or calculate it.",
        "security": [
          {
            "api_key": []
          }
        ],
        "parameters": [
          {
            "in": "body",
            "name": "body",
            "required": true,
            "description": "Natural-language CRM request payload.",
            "schema": {
              "$ref": "#/definitions/AssistantRequest"
            }
          }
        ],
        "responses": {
          "200": {
            "description": "Successful CRM response.",
            "schema": {
              "$ref": "#/definitions/CrmResponse"
            }
          },
          "400": {
            "description": "Invalid CRM request.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "401": {
            "description": "Missing or invalid API key.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "403": {
            "description": "Access denied by the CRM provider.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "409": {
            "description": "Pagination or continuation conflict.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "429": {
            "description": "Rate limit exceeded.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "500": {
            "description": "Unexpected backend error.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          },
          "502": {
            "description": "Upstream CRM or dependency failure.",
            "schema": {
              "$ref": "#/definitions/ErrorResponse"
            }
          }
        }
      }
    }
  },
  "definitions": {
    "AssistantRequest": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "question"
      ],
      "properties": {
        "question": {
          "type": "string",
          "minLength": 1,
          "maxLength": 2000,
          "description": "Pass the user's complete original natural-language CRM request exactly as written. This must be the user's text, such as 'give me next 20 deals'; never pass a conversation ID, request ID, UUID, module, fields, filters, limit, or offset. Do not rewrite, summarize, translate, interpret, construct structured CRM parameters, calculate pagination, or construct COQL."
        },
        "conversation_id": {
          "type": "string",
          "description": "Optional backend conversation identifier used to maintain conversational state for follow-up requests. This is managed by the backend; callers should not synthesize or mutate it."
        },
        "continuation_token": {
          "type": "string",
          "minLength": 1,
          "description": "Optional for a new query. For pagination follow-ups, pass the exact continuation_token returned by the previous CRM response. Never invent, modify, decode, or calculate it."
        }
      }
    },
    "CrmResponse": {
      "type": "object",
      "required": [
        "success",
        "status",
        "request_id",
        "question",
        "answer",
        "module",
        "module_api_name",
        "request_type",
        "count",
        "records",
        "pagination"
      ],
      "properties": {
        "success": {
          "type": "boolean",
          "description": "Whether the CRM request completed successfully."
        },
        "status": {
          "type": "string",
          "description": "Backend status string such as ok or error."
        },
        "request_id": {
          "type": "string",
          "description": "Backend request identifier for tracing and diagnostics."
        },
        "continuation_token": {
          "type": "string",
          "description": "Opaque backend-generated token used to continue the current CRM query. For pagination follow-ups, pass this exact value back to the backend. Never generate, modify, decode, or calculate this value."
        },
        "question": {
          "type": "string",
          "description": "The original natural-language CRM question submitted by the user."
        },
        "answer": {
          "type": "string",
          "description": "Natural-language answer produced from the CRM results."
        },
        "module": {
          "type": "string",
          "description": "Canonical CRM module resolved by the backend."
        },
        "module_api_name": {
          "type": "string",
          "description": "Exact Zoho CRM API module name used by the backend."
        },
        "request_type": {
          "type": "string",
          "description": "Normalized read-only CRM operation type executed by the backend."
        },
        "count": {
          "type": "integer",
          "format": "int32",
          "description": "Total number of matching CRM records for this query."
        },
        "records": {
          "type": "array",
          "description": "Returned CRM record payloads for the current page.",
          "items": {
            "type": "object",
            "additionalProperties": true
          }
        },
        "pagination": {
          "$ref": "#/definitions/Pagination"
        },
        "error": {
          "$ref": "#/definitions/ErrorDetail"
        }
      }
    },
    "Pagination": {
      "type": "object",
      "required": [
        "limit",
        "offset",
        "returned",
        "more_records"
      ],
      "properties": {
        "limit": {
          "type": "integer",
          "format": "int32",
          "description": "Maximum number of records requested for this page."
        },
        "offset": {
          "type": "integer",
          "format": "int32",
          "description": "Current result offset in the overall CRM result set. This is calculated by the backend. Copilot must never calculate offset values."
        },
        "returned": {
          "type": "integer",
          "format": "int32",
          "description": "Number of records returned in this page."
        },
        "more_records": {
          "type": "boolean",
          "description": "Indicates whether additional CRM records remain after this page."
        }
      }
    },
    "ErrorResponse": {
      "type": "object",
      "required": [
        "success",
        "status",
        "request_id",
        "error"
      ],
      "properties": {
        "success": {
          "type": "boolean",
          "enum": [
            false
          ]
        },
        "status": {
          "type": "string",
          "enum": [
            "error"
          ]
        },
        "request_id": {
          "type": "string",
          "description": "Backend request identifier for tracing."
        },
        "error": {
          "$ref": "#/definitions/ErrorDetail"
        }
      }
    },
    "ErrorDetail": {
      "type": "object",
      "required": [
        "code",
        "message"
      ],
      "properties": {
        "code": {
          "type": "string",
          "description": "Stable backend error code."
        },
        "message": {
          "type": "string",
          "description": "Human-readable error message."
        },
        "details": {
          "type": "object",
          "additionalProperties": true,
          "description": "Additional structured error context when available."
        }
      }
    }
  }
}
